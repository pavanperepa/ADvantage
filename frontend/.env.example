# Base URL of the cricket_posts FastAPI backend. Defaults to
# http://127.0.0.1:8000 (`uvicorn cricket_posts.web:app`'s default port) when
# unset. Copy this file to .env.local and adjust if your backend runs
# elsewhere.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
